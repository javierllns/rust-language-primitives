## How run cargo project

#### Gitbash:

`$ cargo run`

## How to compile

#### Gitbash (With Cargo):

`$ cargo build`

#### Gitbash (Without Cargo):

`$ rustc main.rs`

## How to run Compiled

#### Gitbash:

`$ ./main.exe`

## Additional

#### Build the docs (Using Gitbash):

`$ cargo doc --open`
